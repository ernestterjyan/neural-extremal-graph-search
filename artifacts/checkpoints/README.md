# Release checkpoints

These are the best final-stage checkpoints from seeds 0–4 of the completed MVP
curriculum. Each file contains the model state, architecture configuration,
training seed, final curriculum size, selected iteration, and validation score.

Load a checkpoint with `extremal_graph.training.load_model_checkpoint`.
Checkpoint files use PyTorch serialization and should only be loaded from this
trusted repository.
